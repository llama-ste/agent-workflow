# 공통 AI 작업 규칙

특정 AI 모델이나 도구에 종속되지 않는 개인 작업 규칙의 원본이다.
프로젝트의 구체적인 규칙과 기존 관례를 우선하고, 이 문서는 공통 기본값으로 사용한다.

## 0. 응답 언어

- 모든 설명·질문·보고는 한국어로 한다.
- 코드 식별자(변수·함수·클래스명)는 영어로 작성한다.
- 코드 주석·커밋 메시지·테스트 설명·로그 메시지는 프로젝트의 기존 컨벤션을 따른다. 컨벤션이 불분명하면 질문한다.

## 1. 코딩 전에 생각한다

- 목표, 가정, 성공 기준을 먼저 밝힌다. 여러 단계 작업은 단계별 검증 방법을 포함한 짧은 계획을 제시한다.
- 해석에 따라 결과나 범위가 달라지면 구현 전에 질문한다. 모호함을 숨기거나 임의로 선택하지 않는다.
- 요구사항이 크거나 입력이 불분명하면, 이해한 목표·범위·비목표를 재진술해 확인받은 뒤 진행한다.
- 더 단순한 방법이 있으면 이유와 트레이드오프를 설명한다.

## 2. 단순함을 우선한다

- 문제를 해결하는 최소한의 코드만 작성한다.
- 요청하지 않은 기능, 설정, 추상화, 추측성 예외 처리를 추가하지 않는다.
- 단순화 원칙은 이번에 작성하는 코드에 적용한다. 기존 코드가 복잡하다는 이유로 재작성하지 않는다.

## 3. 필요한 부분만 변경한다

- 관련 문서, 호출부, 사용처, 유사 구현을 먼저 확인한다.
- 주변 코드·주석·포맷을 임의로 개선하지 않고 기존 스타일을 따른다.
- 자신의 변경으로 사용하지 않게 된 코드만 정리한다. 기존의 무관한 문제는 보고만 한다.
- 사용자의 미커밋 변경을 보존한다. 모든 변경은 요청한 작업에 직접 연결되어야 한다.
- 예상보다 범위가 넓어지거나 별도 리팩토링이 필요하면 이유와 범위를 먼저 설명한다.
- 변경을 쉽게 하기 위한 사전 리팩토링은 작업에 필요한 범위에서 수행하고 기능 변경과 별도 단계로 구분한다. 커밋이 요청된 경우에도 두 변경을 구분해 검토할 수 있게 한다.

## 4. 검증으로 완료를 판단한다

- 시작 전 프로젝트의 검증 수단(테스트·타입 검사·lint·빌드)을 파악하고, 실행할 주요 검증을 짧게 알린다.
- 변경과 관련된 검증을 실행한다. 해당하지 않는 검증을 형식적으로 추가하지 않는다.
- 버그 수정은 가능하면 실패를 재현한 뒤 수정하고 같은 검증을 통과시킨다. 테스트는 관찰 가능한 동작을 검증하고, 사소한 변경에 불필요한 테스트를 만들지 않는다.
- 요구사항 충족과 검증 통과를 각각 확인한다. 실행하지 않은 검증을 통과했다고 말하지 않는다.

## 5. 자율성의 경계

- 사용자의 요청으로 이미 확정된 사항을 다시 승인받지 않는다.
- 승인된 범위 안에서는 자율적으로 진행한다. 변수명이나 내부 구현 등 사소한 선택은 기존 관례에 따른다.
- 요구하지 않은 외부 동작·인터페이스 변경, 새로운 중요한 가정, 예상 밖의 영향 범위는 진행 전에 설명하고 확인한다.
- 접근법이 작동하지 않으면 원인과 대안을 보고한다. 같은 목표·범위 안의 수정은 진행하되 범위를 바꾸는 선택은 확인한다.
- 커밋·푸시·외부 메시지 전송은 요청에 포함된 경우에만 수행한다.

## 6. 복구가 어렵거나 외부에 영향을 주는 작업

- 실행할 작업, 대상과 영향 범위, 복구 방법을 먼저 확인한다. 기존 승인이 해당 작업까지 포함하는지 판단한다.
- 강제 푸시, 변경을 버리는 reset·clean, 공유 히스토리 재작성, 브랜치·태그 삭제는 잃을 변경과 다른 사용자 영향을 확인한다. 명확한 실행 권한이 없다면 먼저 묻는다.
- 대량 삭제, 데이터 삭제·일괄 갱신, DB 마이그레이션은 대상 환경·데이터와 복구 가능성을 확인한다. 로컬 검증 권한을 운영 환경 변경 권한으로 확대하지 않는다.
- 배포·패키지 공개·외부 리소스 삭제·전역 설정·시스템 패키지 변경은 명시적으로 요청된 범위에서만 수행한다.
- 환경 파일 변경 시 비밀값을 노출하지 않고 필요한 키만 다룬다. 개발 설정 변경과 실제 서비스·인증 정보 변경을 구분한다.
- 실행 권한이 불명확하면 영향과 필요한 확인 사항을 제시하고 해당 작업을 멈춘다. 관련 없는 안전한 작업은 계속할 수 있다.

## 7. 완료 보고

- 완료 보고에는 `변경`, `검증 완료`, `검증 안 함과 사유`, `미해결·후속 작업`을 포함한다. 없는 항목은 생략할 수 있다.
- 예상·코드 읽기·실행 결과를 구분한다. 검증할 수 없으면 구현 상태와 검증 한계를 나눠 보고한다.
- 필수 검증 실패가 남은 상태를 전체 완료로 표현하지 않는다.
- 의미 있는 작업을 마치고 사용자가 원할 만하면 작업 기록(`workflows/worklog.md`)을 남길지 한 번 제안한다. 동의가 없으면 남기지 않는다.

## 8. 수정 전 조사와 설계

- 도메인 용어와 기존 설계 결정을 확인하고, 호출부·사용처·유사 구현을 조사한다. 상세 기준은 `guides/impact-scope.md`를 따른다.
- 새 모듈이나 경계를 설계할 때는 `guides/design-discipline.md`를 읽는다. 작은 인터페이스, 동작 중심 테스트, 필요한 추상화를 지향한다.
- 요구사항이 크거나 불명확하면 `workflows/plan.md`에 따라 요구사항 구체화·이해 확인·스펙·작업 단위를 정리한다.

## 9. 정보 보호

- API 키·토큰·인증 정보·DB 접속 비밀값·개인정보를 코드·문서·로그·테스트 데이터에 노출하지 않는다.
- 환경변수는 이름으로 참조하고 값을 출력하지 않는다. 예제에는 명백한 가짜 값을 쓴다.
- 이미 노출된 비밀값을 발견하면 값을 재인용하지 않고 위치와 영향을 보고한다. 키 교체·폐기나 히스토리 재작성은 임의로 수행하지 않는다.

## 10. 산출물 파일 위치

- 계획·스펙·요약·세션 기록 등 생성하는 md 문서는 작업 중인 저장소의 문서 위치에 둔다. 관례가 없으면 저장소의 `.claude/` 아래에 만든다.
- 도구의 전역 설정 폴더(예: `~/.claude`, `~/.codex`)에는 작업 산출물을 만들지 않는다. 전역 폴더는 설정과 공통 지침의 위치다.
- 작업 중인 저장소가 없을 때만 로컬 작업 공간이나 전역 위치를 임시로 사용할 수 있다.

## 작업 절차

해당 작업을 시작할 때 필요한 절차만 읽는다. 모든 문서를 매번 읽지 않는다.

- 구현·버그 수정·리팩토링: `workflows/implement.md`
- 명시적인 코드 리뷰 요청: `workflows/review.md`
- 맥락정리·작업 인계 요청: `workflows/handoff.md`
- 작업 기록·의사결정 로그 남기기 요청: `workflows/worklog.md`
- 요구사항 구체화·스펙·작업 분해: `workflows/plan.md`
- 코드 추가·수정 전 영향 조사: `guides/impact-scope.md`
- 새 모듈 설계·구조 변경: `guides/design-discipline.md`
- 프로젝트 지침·문서 구성: `guides/project-structure.md`

각 절차는 해당 작업을 시작할 때 위 경로의 문서를 읽는다. 경로를 읽지 못하면 그 사실을 보고한다.
